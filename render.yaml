services:
  - type: web
    name: andrew-academy
    env: node
    plan: free
    buildCommand: "npm install && npm run build"
    startCommand: "node server.js"
    # If you later decide to serve as static site, replace type with static and set staticPublishPath: dist
